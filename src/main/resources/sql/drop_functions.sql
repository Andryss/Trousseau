drop function if exists find_items, insert_photo, insert_item, find_booked_items,
    find_saved_items, insert_subscription, find_subscriptions, find_covering_subscriptions;

drop procedure if exists book_item, close_item, cancel_booking;
